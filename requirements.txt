# No third-party packages needed — the backend only uses Python's
# standard library (json, os, time, urllib, http.server).
# This file must still exist so Vercel detects and builds the /api
# directory as Python serverless functions.
